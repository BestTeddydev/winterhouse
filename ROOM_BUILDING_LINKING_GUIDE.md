# 🔗 Room-Building Linking Guide

## 📋 ภาพรวมการเปลี่ยนแปลง

เราได้ปรับปรุงระบบให้สามารถสร้างห้องพักโดยไม่ต้องผูกกับอาคารก่อน และสามารถมาผูกกับอาคารในภายหลังได้ในหน้า "จัดการแผนผัง"

### 🔄 **Workflow ใหม่**

```
1. สร้างห้องพัก → /admin/rooms/new (ไม่ต้องเลือกอาคาร)
2. สร้างอาคาร → /admin/buildings/new (ถ้าต้องการ)
3. จัดการแผนผัง → /admin/site-map (ผูกห้องพักกับอาคาร)
```

## 🗂️ **การเปลี่ยนแปลง Schema**

### **Room Model**
```typescript
interface IRoom {
  // ... existing fields
  buildingId?: mongoose.Types.ObjectId  // เปลี่ยนเป็น optional
  // ... other fields
}
```

- `buildingId` เปลี่ยนจาก `required: true` เป็น `required: false`
- ห้องพักสามารถมี `buildingId` เป็น `null` หรือ `undefined`

## 🚀 **การเปลี่ยนแปลง API**

### **POST /api/rooms**
- ไม่ต้องส่ง `buildingId` ในการสร้างห้องพัก
- `buildingId` เป็น optional field

### **POST /api/rooms/link-building** (ใหม่)
```javascript
// ผูกห้องพักกับอาคาร
POST /api/rooms/link-building
{
  "roomId": "room_id_here",
  "buildingId": "building_id_here"
}
```

### **DELETE /api/rooms/link-building** (ใหม่)
```javascript
// ยกเลิกการผูกห้องพักกับอาคาร
DELETE /api/rooms/link-building
{
  "roomId": "room_id_here"
}
```

## 🎨 **การเปลี่ยนแปลง UI**

### **หน้า /admin/rooms/new**
- **ลบ**: การเลือกอาคาร (dropdown)
- **เพิ่ม**: หมายเหตุอธิบายว่าไม่ต้องผูกกับอาคารตอนนี้
- **คงไว้**: การอัปโหลดหลายรูป

### **หน้า /admin/site-map**
- **เพิ่ม**: แสดงจำนวนห้องพักที่ยังไม่ได้ผูกกับอาคาร
- **ปรับปรุง**: การผูกห้องพักกับอาคารผ่าน API ใหม่
- **เพิ่ม**: Toast notifications เมื่อผูก/ยกเลิกการผูกห้องพัก

## 🔧 **การใช้งาน**

### **1. สร้างห้องพัก**
```bash
1. ไปที่ /admin/rooms/new
2. กรอกข้อมูลห้องพัก (ไม่ต้องเลือกอาคาร)
3. อัปโหลดรูปภาพ
4. บันทึก
```

### **2. สร้างอาคาร** (ถ้าต้องการ)
```bash
1. ไปที่ /admin/buildings/new
2. กรอกข้อมูลอาคาร
3. กำหนดตำแหน่ง X, Y บนแผนผัง
4. บันทึก
```

### **3. ผูกห้องพักกับอาคาร**
```bash
1. ไปที่ /admin/site-map
2. อัปโหลดแผนผัง (ถ้ายังไม่มี)
3. คลิกที่ตำแหน่งบนแผนผังเพื่อสร้างอาคาร
4. ในรายการห้องพัก ให้เลือก checkbox ห้องพักที่ต้องการ
5. บันทึกแผนผัง
```

## 📊 **สถานะห้องพัก**

### **ห้องพักที่ยังไม่ได้ผูก**
- `buildingId` = `null` หรือ `undefined`
- แสดงในรายการ "ห้องพักที่ใช้ได้" ในหน้า site-map
- สามารถผูกกับอาคารใดก็ได้

### **ห้องพักที่ผูกแล้ว**
- `buildingId` = ObjectId ของอาคาร
- แสดงในรายการห้องพักของอาคารนั้น
- สามารถยกเลิกการผูกได้

## 🎯 **ประโยชน์**

### **1. ความยืดหยุ่น**
- สร้างห้องพักได้โดยไม่ต้องมีอาคารก่อน
- สามารถผูกห้องพักกับอาคารในภายหลัง
- สามารถย้ายห้องพักระหว่างอาคารได้

### **2. การจัดการที่ดี**
- แยกขั้นตอนการสร้างห้องพักและการจัดวางแผนผัง
- ไม่ต้องกังวลเรื่องลำดับการสร้างข้อมูล
- สามารถทดลองจัดวางแผนผังได้หลายแบบ

### **3. User Experience**
- กระบวนการสร้างห้องพักง่ายขึ้น
- ไม่ต้องมีอาคารก่อนถึงจะสร้างห้องพักได้
- สามารถจัดการแผนผังได้อย่างอิสระ

## 🔍 **การตรวจสอบ**

### **ตรวจสอบห้องพักที่ยังไม่ได้ผูก**
```javascript
// API call
GET /api/rooms
// Response จะมี rooms ที่ buildingId เป็น null
```

### **ตรวจสอบห้องพักในอาคาร**
```javascript
// API call
GET /api/buildings/[buildingId]
// Response จะมี rooms ที่ผูกกับอาคารนั้น
```

## ⚠️ **หมายเหตุสำคัญ**

1. **การย้อนกลับ**: ห้องพักเก่าที่มี `buildingId` จะยังคงทำงานได้ปกติ
2. **การ Migration**: ไม่จำเป็นต้อง migrate ข้อมูลเก่า
3. **การแสดงผล**: หน้า `/rooms` จะแสดงเฉพาะห้องพักที่ผูกกับอาคารแล้ว
4. **การจอง**: ระบบจองจะทำงานได้ปกติกับห้องพักที่ผูกกับอาคารแล้ว

## 🛠️ **การพัฒนาต่อ**

- เพิ่มการกรองห้องพักตามสถานะการผูกกับอาคาร
- เพิ่มการแสดงสถิติจำนวนห้องพักที่ยังไม่ได้ผูก
- เพิ่มการย้ายห้องพักระหว่างอาคารแบบ batch
- เพิ่มการตรวจสอบความถูกต้องของการผูกห้องพักกับอาคาร
